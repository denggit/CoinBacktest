我有一个 ETH 永续的新 edge idea：xxx。

请按 CoinBacktest 策略工厂规范写 research 脚本。

背景：
- 当前只研究 ETH-USDT-SWAP / ETH 永续。
- 本阶段只做 research，不做 backtest。
- 目标是判断这个 idea 是否存在可交易 edge。
- 如果没有 edge，后续会直接 rejected，不要硬做成策略。

标识：
1. Edge ID: ETH_EDGE_XXX
2. Experiment ID: ETH_RESEARCH_XXX_V1

文件要求：
3. 脚本放在 research/xxx/xxx_research.py
4. 输出目录放在 data/reports/research/xxx/ETH_RESEARCH_XXX_V1/

输出要求：
5. 必须输出：
   - 00_manifest.json
   - 01_summary.csv
   - 02_event_counts.csv
   - 03_signal_audit.csv
   - 04_forward_returns.csv
   - 05_by_year.csv
   - 06_cost_sensitivity.csv
   - 09_decision_draft.json

架构要求：
6. research 脚本不能 import 其他 research/backtest 脚本。
7. 可复用逻辑必须放 src/。
8. 不允许未来函数。
9. 不允许为了结果好看修改时序。
10. 重数据必须流式/缓存/向量化，不能一次性读爆内存。
11. 加阶段日志和干净进度条，不要逐 job 打断进度条。
12. 输出要方便后续 tools/prepare_gpt_review.py 自动打包给 GPT 审查。