对，`AETHER_STRATEGY=strategies.eth_portfolio_v1:Strategy` 这个入口很好，**建议永久保留这个配置形式**。以后不管加载单策略还是完整 Portfolio，AetherEdge 都只认识一个 `Strategy` 插件实例。

我的建议不是“先修完所有功能”或者“马上进行一次巨大重构”，而是：

> **先修确定性问题并冻结现状，再定义最终架构契约，然后按职责逐步拆解，最后迁移当前 Portfolio。**

顺序不能反。否则边重构边改业务，很难判断问题来自策略、执行还是架构。

---

# 总体路线

```text
阶段0：冻结当前实盘基线
阶段1：修复确定性 Bug
阶段2：先设计并冻结最终插件契约
阶段3：逐步拆分 LiveRuntimeRunner
阶段4：建立 Target Position + 独立 Risk 主链路
阶段5：整理状态和数据库所有权
阶段6：迁移当前 eth_portfolio_v1
阶段7：形成标准插件模板
阶段8：再开发新的 Portfolio 和策略
阶段9：稳定盈利后增加前端、Shadow 等增强功能
```

最重要的是：

> **重构期间不改策略逻辑、不改仓位逻辑、不改止损逻辑、不改多交易所主从规则。**

---

# 阶段0：冻结当前实盘基线

在大改前，先建立一个真正可信的起点。

## 要做的事

1. 保存当前服务器正在运行的 commit。
2. 确认当前工作区是否有未提交修改。
3. 记录当前部署配置。
4. 跑完整测试。
5. 保存当前实盘启动日志。
6. 记录现有启动和恢复流程。
7. 记录现有持仓、挂单、止损接管行为。
8. 明确 OKX 主、Binance 跟随的现有行为。
9. 给当前架构标记一个稳定版本，例如：

```text
AetherEdge Legacy Runtime Baseline V1
```

## 为什么必须先做

后面每次重构都要和这个基线对比：

```text
输入相同事件
→ 策略决策是否相同
→ 下单意图是否相同
→ 止损是否相同
→ 恢复结果是否相同
```

没有基线，大重构以后即使测试通过，也不知道行为是否偷偷变化。

---

# 阶段1：先修复确定性问题

这一步不做架构大改，只处理已经明确存在的问题。

## P0-01：修复 `.env.example` 加载

正确优先级：

```text
系统环境变量
> 项目 .env
> 非敏感默认配置
```

必须保证：

```text
.env.example 永远不参与运行时配置加载
```

同时 live 模式拒绝：

```text
your_api_key
你的_okx_api_key
changeme
placeholder
xxx
```

这种占位凭证。

这个问题需要最先修，因为它已经导致测试失败，也可能造成错误配置覆盖。

---

## P0-02：补齐生产依赖

把生产代码实际使用的依赖完整声明，包括当前缺失的 `pandas`。

建议最后收口成：

```text
pyproject.toml
+
lock 文件
```

至少不能再出现：

```text
本地环境能跑
服务器新环境安装 requirements 后却缺包
```

---

## P0-03：修复逻辑幂等 ID

当前信号和订单幂等不能依赖每次运行时的墙上时间。

改为稳定的逻辑 ID：

```text
strategy_id
+ edge_id
+ symbol
+ signal_bar_close_time
+ action
+ position_generation
+ sequence
```

这样进程重启、事件重放、恢复后重复决策，都不会重复开仓。

---

## P0-04：所有测试恢复全绿

阶段1完成标准：

```text
完整测试全绿
配置测试全绿
当前策略输出不变
当前下单行为不变
当前恢复行为不变
```

此时再打一个干净基线 commit。

---

# 阶段2：先定义最终架构契约

不要一上来就拆 `LiveRuntimeRunner`。

先明确未来所有模块靠什么接口协作，否则只是在把一大坨代码搬成很多小文件。

## 保留现有加载方式

继续使用：

```text
AETHER_STRATEGY=strategies.eth_portfolio_v1:Strategy
```

未来新 Portfolio 仍然是：

```text
AETHER_STRATEGY=strategies.eth_copy_portfolio_v2:Strategy
```

框架入口不变。

---

## 定义四个稳定版本化契约

### 1. `StrategyPluginV1`

插件顶级接口：

```python
class StrategyPluginV1(Protocol):
    def manifest(self) -> StrategyManifestV1:
        ...

    def requirements(self) -> StrategyRequirementsV1:
        ...

    async def on_event(
        self,
        context: StrategyContextV1,
        event: StrategyEventV1,
    ) -> StrategyDecisionV1 | None:
        ...

    def snapshot_state(self) -> StrategyStateSnapshotV1:
        ...

    def restore_state(
        self,
        snapshot: StrategyStateSnapshotV1,
    ) -> None:
        ...
```

---

### 2. `StrategyRequirementsV1`

插件声明自己需要的数据：

```text
1m K线
5m K线
4H K线
raw trades
1s trade bar
range bar
footprint
order book
funding
OI
warmup长度
```

Runtime 根据声明启动通用数据服务。

以后增加策略数据需求时，不需要修改 Runner。

---

### 3. `StrategyDecisionV1`

插件最终输出的是：

```text
目标仓位
目标风险
保护止损
决策原因
贡献 edge
```

而不是直接输出：

```text
买入多少 ETH
```

---

### 4. `StrategyStateSnapshotV1`

插件只返回自己的业务状态：

```text
regime
edge armed状态
cooldown
虚拟 sleeve
逻辑入场价格
trailing状态
```

架构负责保存，不允许插件随意创建不同 SQLite。

---

# 阶段3：逐步拆分 `LiveRuntimeRunner`

这一步只重构代码组织，保持现有行为。

不要同时引入新 Portfolio 接口。

## 推荐拆分顺序

### R1：抽出生命周期

```text
src/runtime/lifecycle/
    startup.py
    shutdown.py
    service.py
```

负责：

* 启动顺序；
* 关闭顺序；
* readiness；
* graceful shutdown。

---

### R2：抽出 Worker 监督

```text
src/runtime/supervision/
    worker_supervisor.py
    heartbeat.py
    health.py
```

负责：

* worker 启停；
* worker 崩溃；
* heartbeat；
* 任务健康状态。

---

### R3：抽出行情事件分发

```text
src/runtime/orchestration/
    event_dispatcher.py
```

只负责：

```text
接收事件
→ 分类
→ 投递到对应处理器
```

不做策略判断。

---

### R4：抽出 Closed-Bar 调度

```text
src/runtime/orchestration/
    decision_scheduler.py
```

负责：

* bar 是否已关闭；
* 数据是否完整；
* 事件排水；
* 是否允许调用策略；
* 防止重复决策。

---

### R5：抽出恢复协调

```text
src/runtime/recovery/
    coordinator.py
```

负责调用：

* 账户恢复；
* 仓位恢复；
* 订单恢复；
* 止损恢复；
* 策略状态恢复；
* 数据 checkpoint 恢复。

它只编排，不写具体恢复算法。

---

### R6：抽出执行流程

```text
src/runtime/orchestration/
    execution_pipeline.py
```

现阶段仍保持：

```text
TradeSignal
→ Planner
→ OrderIntent
→ OrderCoordinator
```

先把旧流程搬出来，不急着改接口。

---

## Runner 最终只保留

```python
class LiveRuntimeRunner:
    async def run(self) -> None:
        await self.lifecycle.start()
        await self.live_session.run()
        await self.lifecycle.stop()
```

阶段3的验收标准：

```text
LiveRuntimeRunner明显变薄
所有现有行为不变
现有策略不需要修改
现有实盘配置不需要修改
每抽一个模块就单独提交和测试
```

---

# 阶段4：建立未来真正稳定的主链路

Runner 拆干净后，再引入新的策略输出方式。

## 新主链路

```text
Strategy Plugin
→ StrategyDecision
→ Account Risk Engine
→ Approved Target
→ Position Reconciler
→ Position Delta
→ OrderIntent
→ OKX/Binance Execution
```

---

## 4.1 新增 Strategy Host

```text
src/strategy/
    contracts/
    sdk/
    loader.py
    host.py
```

`StrategyHost` 负责：

* 根据 `AETHER_STRATEGY` 加载插件；
* 校验接口版本；
* 调用插件；
* 捕获插件异常；
* 保存和恢复插件状态；
* 记录策略决策审计。

Runtime 不直接调用具体 Portfolio 类。

---

## 4.2 新增通用 Strategy SDK

```text
src/strategy/sdk/
    edge.py
    portfolio.py
    targets.py
    allocation.py
    audit.py
```

这里提供机制，不提供具体策略：

* `EdgeProposal`
* `EdgeContribution`
* `PositionTarget`
* signed exposure 聚合；
* contributor 跟踪；
* 标准审计模型。

具体 Portfolio 可以使用这些工具，但组合规则仍在插件内部。

---

## 4.3 新增账户级 Risk Engine

```text
src/risk/
    models.py
    ports.py
    service.py
    exposure.py
    margin.py
    drawdown.py
    guards.py
```

只负责账户硬风险：

* 最大净敞口；
* 最大毛敞口；
* 最大保证金占用；
* 单日亏损限制；
* 最大回撤降仓；
* 止损覆盖；
* 强平距离；
* 数据过期；
* OKX/Binance 不一致；
* 交易所异常。

输出只允许：

```text
APPROVE
REDUCE
REJECT
```

不能改变策略方向。

---

## 4.4 新增 Position Management

```text
src/position_management/
    models.py
    reconciler.py
    delta.py
    service.py
```

负责：

```text
批准后的目标仓位
-
当前交易所实际仓位
=
需要执行的仓位变化
```

这个层对幂等、重启恢复和多 edge 组合非常关键。

---

## 4.5 保留旧策略兼容层

不要立刻强制当前策略改新接口。

增加：

```text
LegacyTradeSignalAdapter
```

让当前 `eth_portfolio_v1` 仍然可以运行旧流程。

此时新旧两个接口都存在：

```text
旧插件 → TradeSignal
新插件 → StrategyDecisionV1
```

Runtime 统一通过 Strategy Host 处理。

---

# 阶段5：整理状态和数据库

这一步必须放在模块职责明确之后。

现在直接合并 DB，很可能把不属于同一领域的数据又混到一起。

## 先做状态所有权清单

| 数据            | 唯一所有者                         |
| ------------- | ----------------------------- |
| 市场行情          | `market_data`                 |
| 实际订单与成交       | `order_management`            |
| 实际仓位          | `position_management/account` |
| 策略业务状态        | `strategy_host`               |
| 风险状态          | `risk`                        |
| 恢复 checkpoint | `runtime/recovery`            |
| PnL 归因        | `accounting`                  |

---

## 然后做

1. 每个状态明确 source of truth；
2. 统一 `strategy_id`；
3. 统一 `decision_id`；
4. 统一 `event_id`；
5. 统一 `position_generation`；
6. 统一 schema version；
7. 建立 migration registry；
8. 删除真正重复的 store；
9. 明确跨 store 崩溃恢复流程。

不一定要把所有数据放在一个 DB。

重点是：

> 每份状态只能有一个权威所有者。

---

# 阶段6：迁移当前 `eth_portfolio_v1`

新架构完成后，才迁移当前实盘插件。

而且迁移时：

```text
不改策略条件
不改参数
不改 sizing
不改止损
不改退出
不改 OKX/Binance 行为
```

只改变接口表达：

```text
旧：
Portfolio → TradeSignal

新：
Portfolio → StrategyDecisionV1
```

## 必须做行为一致性测试

对同一段历史事件回放：

```text
旧插件决策
vs
新插件决策
```

比较：

* 决策时间；
* 方向；
* 仓位；
* 止损；
* 加仓；
* 减仓；
* 平仓；
* edge 来源；
* 重启恢复结果。

迁移通过后，才允许服务器切换。

---

# 阶段7：形成标准 Portfolio 插件模板

以后新 Portfolio 全部照模板开发：

```text
strategies/
    eth_copy_portfolio_v2/
        plugin.py
        manifest.json
        config.json

        portfolio/
            controller.py
            allocation.py
            conflict.py
            regime_router.py

        edges/
            edge_a/
            edge_b/
            edge_c/

        features/
        exits/
        state/
        audit/
        tests/
```

新策略上线流程固定为：

```text
CoinBacktest研究通过
→ 生成/迁移 Edge
→ 放入 Portfolio 插件
→ 实现 StrategyPluginV1
→ 配置 AETHER_STRATEGY
→ 测试
→ 实盘
```

以后不应该修改：

```text
src/runtime
src/risk
src/order_management
src/platform
```

除非新增的是框架本身不支持的新通用能力。

---

# 阶段8：再开始设计新的 Portfolio

完成标准插件模板后，才开始开发你刚刚说的：

* 趋势突破；
* 趋势回踩；
* 均值回归；
* 流动性反转；
* 波动率启动；

这些具体 edge。

否则现在边研究策略边重构架构，很容易让具体策略需求污染通用框架。

---

# 阶段9：明确延期的功能

这些现在不做：

## Paper / Shadow

按你的决定延期。

等存在稳定完整 Portfolio、以后要在线验证新版本时再做。

## 实盘前端

等主系统稳定赚钱以后再做：

```text
API
+ Dashboard
+ 远程访问
```

现在只保留：

* 日志；
* heartbeat；
* 邮件告警；
* 结构化健康状态。

## 高可用和热备

资金规模和带单规模上来后再做，不要现在把系统复杂度再抬高。

---

# 最终优先级

## 现在立即做

```text
1. 冻结当前基线
2. 修复 .env.example
3. 补齐依赖
4. 修复幂等 ID
5. 所有测试恢复全绿
```

## 然后做

```text
6. 定义并冻结 StrategyPluginV1 契约
7. 逐步拆分 LiveRuntimeRunner
8. 建立 StrategyHost
9. 建立独立 Risk Engine
10. 建立 Target Position / Position Reconciler
```

## 再然后做

```text
11. 整理状态与数据库
12. 迁移当前 eth_portfolio_v1
13. 建立标准 Portfolio 模板
14. 开发新的 Portfolio
```

## 最后做

```text
15. Accounting与收益归因
16. 可观测性前端
17. Shadow/Paper
18. 灾备和高可用
```

---

# 最关键的工程原则

以后每一轮只做一个职责明确的小任务：

```text
一次只抽一个模块
一次只建立一个接口
一次不同时修改架构和策略
每一步都保持旧路径可运行
每一步完整测试
每一步都可以安全回滚
```

不要进行“巨型重写”。

最终你可以继续保持现在的使用方式：

```text
AETHER_STRATEGY=strategies.eth_portfolio_v1:Strategy
```

未来只需要改成：

```text
AETHER_STRATEGY=strategies.eth_copy_portfolio_v2:Strategy
```

而 AetherEdge 的行情、风险、订单、恢复、对账和多交易所执行框架不再随策略变化。
