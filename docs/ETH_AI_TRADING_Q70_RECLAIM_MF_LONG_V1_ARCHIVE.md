# ETH Q70 Reclaim MF Long V1 — Archive Runbook

Apply this patch at repository root. The archive is documentation and lifecycle metadata only; there is no execution command and no live deployment path.

Authoritative entry:

```text
research/eth_ai_trading/archived_models/eth_q70_reclaim_mf_long_v1/README.md
```

Validation:

```bat
python -m pytest testsi_research	est_eth_q70_reclaim_archive.py -q
```
